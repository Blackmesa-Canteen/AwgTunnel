id: io.github.awgtunnel.AwgTunnel
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.golang
command: awg-tunnel

# Every permission here is load-bearing; see README.md for the rationale.
# Notably absent: any --filesystem (profile import uses the file chooser
# portal), any --device beyond the GPU, and any system bus access. The tunnel
# runs in userspace, so no privileged host component is involved.
finish-args:
  - --share=ipc
  - --share=network
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  - --talk-name=org.freedesktop.secrets

cleanup:
  - /include
  - /lib/pkgconfig
  - /man
  - /share/man
  - '*.a'
  - '*.la'

modules:
  - name: awg-tunnel-engine
    buildsystem: simple
    build-options:
      append-path: /usr/lib/sdk/golang/bin
      env:
        # Vendored modules only, and never fetch a toolchain: the build has no
        # network access and must not silently acquire one.
        GOFLAGS: -mod=vendor -trimpath
        GOTOOLCHAIN: local
        CGO_ENABLED: '0'
        GOPATH: /run/build/awg-tunnel-engine/.gopath
        GOCACHE: /run/build/awg-tunnel-engine/.gocache
    build-commands:
      - go build -ldflags=-s -o awg-tunnel-engine ./cmd/wireproxy
      - install -Dm755 awg-tunnel-engine /app/bin/awg-tunnel-engine
      - install -Dm644 LICENSE /app/share/licenses/wireproxy-awg/LICENSE
    sources:
      # Pinned to wireproxy-awg v1.0.18 (84f4795ea76f9c3168a61e478d0fe0e5c3238308)
      # with Go modules vendored by scripts/fetch-engine.sh.
      - type: dir
        path: vendor/wireproxy-awg

  - name: awg-tunnel
    buildsystem: meson
    sources:
      - type: dir
        path: .
        skip:
          - .flatpak-builder
          - .git
          - .work
          - build
          - repo
          - vendor
